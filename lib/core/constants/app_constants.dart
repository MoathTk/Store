class AppConstants {
  AppConstants._();

  static const String appName = 'Store Management';
  static const Duration animationDuration = Duration(milliseconds: 1800);
  static const Duration transitionDuration = Duration(milliseconds: 600);
  static const double formMaxWidth = 400;
  static const double buttonHeight = 56;
}
